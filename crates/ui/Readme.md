
The `ui` crate compiles with the minimal set of code needed with the dock mod as the main driver.

The idea is to be able to just start out with `dock` and use that code standalone in a simple app.

So we will just have the *dock* code as a minimal set upon which to test from.
